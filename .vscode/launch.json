{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "preLaunchTask": "Build",
            "name": "Debug",
            "program": "${workspaceFolder}/breakout/build/debug.exe",
            "args": [],
            "cwd": "${workspaceFolder}/breakout"
        }
    ]
}